<div class="col-xs-12 col-sm-6 col-lg-12">
  <div class="sidebar-content margin-top">
    <h2><i class="fa fa-font-awesome"></i> Favorite companies</h2>
    <hr>
    <% if user.favorite_companies %>
      <% user.favorite_companies.each do |owner, frequency| %>
        <div class="flex-box fav-company">
          <div class="avatar-small" style="background-image: url('<%= cl_image_path (owner.get_profile_picture) %>')">
          </div>
          <div>
            <h4><strong><%= owner.full_name %></strong></h4>
            <h4><%= frequency %> contributions</h4>
          </div>
        </div>
      <% end %>
    <% else %>
      <h4>No contributions yet</h4>
    <% end %>
  </div>
</div>
