<div class="shadow-box margin-top">
  <div class="box-title">
    <p>Participants:</p>
  </div>
  <div class="render">
  <% if project.participations.length > 0 then %>
    <% project.participations.each do |participation| %>
      <%= link_to (user_path(participation.user)) do %>
        <div class="col-lg-1 shift-contributor-left">
            <div class="avatar-medium" style="border: solid white 3px; background-image: url('<%= cl_image_path participation.user.get_profile_picture %>')" data-toggle="popover", data-title="<%= participation.user.full_name %>", placement: "bottom" , data-content="<%= participation.user.description[0..100]%>">
            </div>
          </div>
        <% end %>

    <% end %>
  <% else %>
    <p>There are no participants right now!</p>
  <% end %>
  </div>
</div>
